query projectCollaborators($projectId: Int!) {
  projectCollaborators(projectId: $projectId) {
    id
    project {
      id
    }
    user {
      id
      email
    }
    invitedBy {
      id
      email
    }
    email
    accessLevel
    created
    createdById
    modified
    modifiedById
  }
}
